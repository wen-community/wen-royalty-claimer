import { MediaQuery } from "../types";
import { MEDIA } from "./media";

export const APP_MAX_WIDTH = "1000px";
export const NAV_BREAKPOINT: MediaQuery = MEDIA.MD;
export const NAV_HEIGHT = 83.5;
export const NAV_HEIGHT_PX = NAV_HEIGHT + "px";

export const APP_PADDING = { mobile: 3, desktop: 4 };
